# AgeTalker 语音合成（TTS）系统说明文档

> **版本**: v5 | **更新日期**: 2026-07-07 | **TTS 引擎**: 阿里云 DashScope CosyVoice v1

---

## 1. 概述

### 1.1 在流水线中的位置

语音合成（Text-to-Speech，TTS）是 AgeTalker 四步流水线的 **第四步（STEP 4）**，负责将 LLM 生成的心理陪伴文本合成为语音，通过扬声器播放给老年用户。

```
STEP 1          STEP 2           STEP 3              STEP 4
用户语音 ──→ ASR 语音识别 ──→ Emotion 情绪识别 ──→ LLM 心理回复 ──→ TTS 语音合成 ──→ 扬声器播放
(WebSocket)    (funasr)         (emotion2vec)       (DeepSeek+Qwen)   (CosyVoice)
```

### 1.2 核心设计理念：愈合对称（Healing Symmetry）

TTS 参数不是随机选择或固定不变的，而是根据用户的 **心理状态类别**（由路由 LLM 判定）选择 **对应的/补偿性的** 语音风格：

| 用户状态 | TTS 风格 | 设计意图 |
|----------|----------|----------|
| 抑郁 / 哀伤 | 温柔、低速、低音调 | 给予温暖、安全、不催促的陪伴感 |
| 焦虑 | 平静、略低速 | 用沉稳的声音帮助用户平复 |
| 愤怒 | 温柔、略低速 | 安抚而非激化 |
| 孤独 | 温柔、低速 | 温暖陪伴，填补孤独感 |
| 积极 | 轻快、略高速 | 匹配用户的正面情绪 |
| 高危 | 最温柔、最低速 | 极度安抚，降低刺激 |

### 1.3 完整数据流

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              后端 (Python FastAPI)                                │
│                                                                                  │
│  LLMService.stream_reply()                                                       │
│    │                                                                             │
│    ├─ 路由 LLM (DeepSeek) 判定 category + strategy_id                            │
│    ├─ 生成 LLM (Qwen) 流式生成回复文本                                            │
│    └─ 在 SSE "done" 事件中携带 tts_params: { speed, pitch, style }               │
│                                                                                  │
│  SSE /llm/stream ──── data: {"type":"done", "tts_params":{...}, ...}             │
│                                                                                  │
│  TTSService.synthesize_stream(text, speed, pitch, style, emotion_label)          │
│    │                                                                             │
│    ├─ 参数预处理（钳位 speed/pitch、查表 style/voice）                            │
│    ├─ 创建 CosyVoice SpeechSynthesizer 会话                                       │
│    ├─ 分句 → streaming_call() 逐句送入                                             │
│    ├─ streaming_complete() 结束输入                                               │
│    └─ Queue 收集 PCM 数据 → yield 给 StreamingResponse                            │
│                                                                                  │
│  HTTP POST /tts/stream ──── audio/pcm (24kHz 16bit mono)                         │
└──────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              前端 (React Native / Expo)                          │
│                                                                                  │
│  useLLM.fetchReply()                                                             │
│    │                                                                             │
│    ├─ 解析 SSE 流，累积 delta 文本                                                │
│    ├─ 从 "done" 事件提取 tts_params + category                                   │
│    └─ 调用 onSentence(fullText, ttsParams, category)                             │
│                                                                                  │
│  useTTS.speak(text, ttsParams, emotionLabel)                                     │
│    │                                                                             │
│    ├─ 加入播放队列 playQueueRef                                                   │
│    ├─ processQueue() → playOnce() 逐个播放                                       │
│    ├─ POST /tts/stream 获取 PCM 流                                               │
│    ├─ Int16 PCM → Float32 → AudioBuffer                                          │
│    └─ BufferSource → AudioContext.destination 播放                                │
│                                                                                  │
│  回音消除: tts-start 事件 → useASR 静音麦克风 → tts-end 事件 → 200ms 后恢复        │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 后端 TTS 服务

### 2.1 配置层 (`backend/config.py`)

#### 2.1.1 CosyVoice API 配置

```python
# 文件: backend/config.py, 行 173-175
TTS_MODEL         = 'cosyvoice-v1'                    # TTS 模型名称
TTS_DEFAULT_VOICE = os.getenv('TTS_DEFAULT_VOICE', 'longxiaochun')  # 默认音色
```

API 密钥通过环境变量 `DASHSCOPE_API_KEY` 注入：
```python
# 文件: backend/config.py, 行 99
DASHSCOPE_API_KEY = os.getenv('DASHSCOPE_API_KEY', '')
```

#### 2.1.2 音频格式规范

```python
# 文件: backend/config.py, 行 178-180
TTS_SAMPLE_RATE = 24000   # CosyVoice 输出 24kHz
TTS_CHANNELS    = 1       # 单声道
TTS_BIT_DEPTH   = 16      # 16bit PCM
```

前端也有对应的常量定义：
```typescript
// 文件: mobile/constants/TTS.ts
export const TTS_CONFIG = {
  SAMPLE_RATE: 24000,
  CHANNELS: 1,
  BIT_DEPTH: 16,
  BASE_URL: 'http://localhost:8050',
};
```

#### 2.1.3 参数范围限制

```python
# 文件: backend/config.py, 行 183-186
TTS_SPEED_MIN = 0.5    # 最慢 0.5x
TTS_SPEED_MAX = 2.0    # 最快 2.0x
TTS_PITCH_MIN = -12    # 最低 -12 半音
TTS_PITCH_MAX = 12     # 最高 +12 半音
```

#### 2.1.4 风格 → CosyVoice 标签映射

将系统内部风格名映射到 CosyVoice API 的情感标签：

```python
# 文件: backend/config.py, 行 189-194
TTS_STYLE_MAP = {
    'gentle':   'gentle',
    'calm':     'calm',
    'cheerful': 'cheerful',
    'neutral':  'neutral',
}
```

当风格为非 `neutral` 时，会作为 `additional_params={'emotion': style_label}` 传给 CosyVoice API。

#### 2.1.5 情绪 → 音色映射

```python
# 文件: backend/config.py, 行 197-206
TTS_EMOTION_VOICE_MAP = {
    'happy':     'longxiaochun',
    'neutral':   'longxiaochun',
    'sad':       'longxiaochun',
    'fearful':   'longxiaochun',
    'angry':     'longxiaochun',
    'disgusted': 'longxiaochun',
    'surprised': 'longxiaochun',
    '_crisis':   'longxiaochun',
}
```

> **设计说明**：当前所有情绪统一使用 `longxiaochun` 音色（温柔女声）。情感表达通过 **语速/音调/风格** 三个参数的变化来实现，而非切换音色。这样做的好处是保持声音的一致性，避免频繁切换音色带来的不自然感。

#### 2.1.6 分句阈值

```python
# 文件: backend/config.py, 行 209
TTS_MAX_SENTENCE_LEN = 50  # 单句最大字符数（用于分句参考）
```

---

### 2.2 核心服务 (`backend/services/tts_service.py`)

#### 2.2.1 TTSCallback 类

桥接 DashScope 的异步回调到 Python 同步 `Queue`，实现流式音频输出。

```python
class TTSCallback(ResultCallback):
    """将 DashScope 的异步回调事件桥接到 Python Queue。"""

    def __init__(self, q: queue.Queue):
        self.q = q

    def on_open(self):
        """流已打开，记录日志。"""

    def on_data(self, data: bytes):
        """收到音频数据块，直接放入队列。"""

    def on_complete(self):
        """合成完成，将 None 放入队列作为结束哨兵。"""

    def on_error(self, message):
        """合成出错，将 Exception 对象放入队列。"""

    def on_close(self):
        """流关闭，记录日志。"""
```

**关键设计**：
- `on_data()` 将每个 PCM 音频块放入 Queue
- `on_complete()` 放入 `None` 作为流结束信号（哨兵模式）
- `on_error()` 放入 `Exception` 对象，主循环检测后抛出

#### 2.2.2 TTSService 类

```python
class TTSService:
    """CosyVoice TTS 语音合成服务 (STEP 4)。"""

    def __init__(self):
        self.is_playing: bool = False  # 当前是否正在合成
```

##### synthesize_stream() — 核心流式合成方法

```python
def synthesize_stream(
    self,
    text:          str,       # 待合成文本
    speed:         float = 1.0,  # 语速倍率 0.5~2.0
    pitch:         int   = 0,    # 音调偏移 -12~12（半音）
    style:         str   = 'neutral',  # 情感风格标签
    emotion_label: str   = 'neutral',  # 用户情绪标签（用于选音色）
) -> Generator[bytes, None, None]:
    """流式语音合成，yield 原始 PCM 24kHz 16bit 单声道字节。"""
```

**内部处理流程**：

1. **参数预处理**：
   ```python
   speed = max(TTS_SPEED_MIN, min(TTS_SPEED_MAX, float(speed)))   # 钳位到 [0.5, 2.0]
   pitch_rate = round(2.0 ** (pitch / 12.0), 2)                    # 半音 → 倍率转换
   style_label = TTS_STYLE_MAP.get(style, 'neutral')               # 风格标签查表
   voice = TTS_EMOTION_VOICE_MAP.get(emotion_label, TTS_DEFAULT_VOICE)  # 音色选择
   ```

2. **创建合成器会话**：
   ```python
   synthesizer = SpeechSynthesizer(
       model             = TTS_MODEL,          # 'cosyvoice-v1'
       voice             = voice,              # 音色名
       format            = AudioFormat.PCM_24000HZ_MONO_16BIT,
       speech_rate       = speed,              # 语速
       pitch_rate        = pitch_rate,         # 音调倍率
       callback          = callback,           # TTSCallback 实例
       additional_params = {'emotion': style_label} if style_label != 'neutral' else None
   )
   ```

3. **分句并逐句送入合成**：
   ```python
   sentences = self._split_sentences(text)
   for sentence in sentences:
       synthesizer.streaming_call(sentence)   # 在同一个会话内逐个发送句子
   synthesizer.streaming_complete()           # 标记输入结束
   ```

4. **从 Queue 收集并 yield 音频块**：
   ```python
   while True:
       item = q.get()
       if item is None:        # 哨兵 → 结束
           break
       if isinstance(item, Exception):
           raise item          # 错误 → 抛出
       yield item              # 音频数据 → 产出
   ```

> **设计要点**：所有句子在**同一个 `SpeechSynthesizer` 会话**内通过 `streaming_call()` 发送，再用一次 `streaming_complete()` 结束。这样 CosyVoice 可以跨句子保持语流连贯性（prosodic coherence），避免每个句子独立合成造成的语气断裂。

##### synthesize_full() — 非流式便捷方法

```python
def synthesize_full(self, *args, **kwargs) -> bytes:
    """非流式合成，返回完整 PCM 字节。"""
    return b''.join(list(self.synthesize_stream(*args, **kwargs)))
```

##### _split_sentences() — 分句算法

```python
def _split_sentences(self, text: str) -> list[str]:
    """
    按中英文句末标点分句。
    匹配模式: [任意非标点字符]+[句末标点]?
    支持标点: 。！？ . ! ? 和换行符
    """
    pattern = r'([^。！？.!?\n]+[。！？.!?\n]?)'
    matches = re.findall(pattern, text)
    if not matches:
        return [text]  # 无匹配时返回整个文本
    return [m.strip() for m in matches if m.strip()]
```

示例：
```
输入: "我理解你的感受。这不是你的错。我会一直陪着你。"
输出: ["我理解你的感受。", "这不是你的错。", "我会一直陪着你。"]
```

---

### 2.3 HTTP 路由 (`backend/routers/stream_tts.py`)

#### 2.3.1 TTSRequest 请求模型

```python
class TTSRequest(BaseModel):
    text:          str   = Field(...,             description="待合成文本")
    speed:         float = Field(default=1.0,     description="语速倍率 0.5~2.0")
    pitch:         int   = Field(default=0,       description="音调偏移 -12~12（半音）")
    style:         str   = Field(default='neutral', description="情感风格")
    emotion_label: str   = Field(default='neutral', description="用户情绪标签，用于自动选音色")
```

两个字段验证器：
- `validate_speed`: 确保 `0.5 <= speed <= 2.0`
- `validate_pitch`: 确保 `-12 <= pitch <= 12`

#### 2.3.2 POST `/tts/stream` — 流式语音合成

```python
@router.post('/tts/stream')
async def tts_stream(req: TTSRequest):
```

- **请求体**: JSON（`TTSRequest` 模型）
- **响应类型**: `audio/pcm`（`StreamingResponse`）
- **响应头**:
  - `X-Sample-Rate: 24000`
  - `X-Channels: 1`
  - `X-Bit-Depth: 16`
  - `Cache-Control: no-cache`
  - `X-Accel-Buffering: no`（禁用 Nginx 缓冲代理）

- **错误状态**:
  - `503`: TTS 服务未初始化
  - `400`: text 为空

#### 2.3.3 GET `/tts/status` — 状态查询

```python
@router.get('/tts/status')
async def tts_status():
```

- **响应**: `{"is_playing": bool, "status": "not_initialized" | "idle"}`

---

## 3. TTS 参数决策机制

### 3.1 心理类别驱动 vs 声学情绪驱动

AgeTalker 维护了 **两套** TTS 参数映射表：

| 映射表 | 键 | 用途 | 状态 |
|--------|-----|------|------|
| `TTS_PARAMS_MAP` | 声学情绪标签（sad, angry, happy...） | 基于 emotion2vec 的声学情绪 | **遗留**，主路径不再使用 |
| `CATEGORY_TTS_PARAMS_MAP` | 心理类别（depression, anxiety, anger...） | 基于路由 LLM 的语义判断 | **当前主路径** |

**为什么使用心理类别而非声学情绪？**

声学情绪（emotion2vec 输出）仅反映语音的 **声学特征**（语调、语速、音高），而心理类别（路由 LLM 输出）反映了用户的 **真实心理状态**。两者可能不一致（例如：用户用平静的语调说"我今天很难过"）。使用心理类别驱动 TTS 可以更精准地匹配用户的真实情绪需求。

### 3.2 CATEGORY_TTS_PARAMS_MAP — 语义驱动参数表

```python
# 文件: backend/config.py, 行 161-171
CATEGORY_TTS_PARAMS_MAP = {
    'depression': {'speed': 0.82, 'pitch': -2, 'style': 'gentle'},    # 抑郁→温柔低速
    'anxiety':    {'speed': 0.88, 'pitch': -1, 'style': 'calm'},      # 焦虑→平静中速
    'anger':      {'speed': 0.88, 'pitch': -1, 'style': 'gentle'},    # 愤怒→温柔抚慰
    'loneliness': {'speed': 0.85, 'pitch': -2, 'style': 'gentle'},    # 孤独→温暖陪伴
    'grief':      {'speed': 0.82, 'pitch': -2, 'style': 'gentle'},    # 哀伤→温柔低速
    'positive':   {'speed': 1.05, 'pitch':  1, 'style': 'cheerful'},  # 积极→明快
    'neutral':    {'speed': 1.00, 'pitch':  0, 'style': 'neutral'},   # 中性→正常
    'crisis':     {'speed': 0.82, 'pitch': -2, 'style': 'gentle'},    # 高危→最温柔
}
```

**设计原则（愈合对称）**：
- 负面情绪（抑郁、哀伤、孤独）：使用 **低速 (0.82-0.85) + 降低音调 (-2) + 温柔风格**，营造安全、温暖、不催促的氛围
- 焦虑/愤怒：使用 **略低速 (0.88) + 微降音调 (-1) + 平静/温柔风格**，帮助用户平复
- 积极情绪：使用 **略高速 (1.05) + 微升音调 (+1) + 明快风格**，匹配和强化正面情绪
- 高危：使用 **最慢速 (0.82) + 最低音 (-2) + 温柔风格**，极度安抚

### 3.3 TTS_PARAMS_MAP — 声学驱动参数表（遗留）

```python
# 文件: backend/config.py, 行 149-158
TTS_PARAMS_MAP = {
    'sad':       {'speed': 0.85, 'pitch': -2, 'style': 'gentle'},
    'fearful':   {'speed': 0.88, 'pitch': -1, 'style': 'calm'},
    'angry':     {'speed': 0.88, 'pitch': -1, 'style': 'gentle'},
    'happy':     {'speed': 1.05, 'pitch':  1, 'style': 'cheerful'},
    'disgusted': {'speed': 0.92, 'pitch': -1, 'style': 'calm'},
    'surprised': {'speed': 1.00, 'pitch':  0, 'style': 'neutral'},
    'neutral':   {'speed': 1.00, 'pitch':  0, 'style': 'neutral'},
    '_crisis':   {'speed': 0.82, 'pitch': -2, 'style': 'gentle'},
}
```

该表对应 `LLMService._get_tts_params()` 方法，保留在代码中供参考，当前主路径不使用。

### 3.4 TTS 参数如何确定

在 `LLMService.stream_reply()` 中（`backend/services/llm_service.py`），SSE 的 `done` 事件通过以下方式确定 TTS 参数：

```python
# 文件: backend/services/llm_service.py, 行 364
tts_params = self._get_tts_params_by_category(category, crisis)
```

```python
# 文件: backend/services/llm_service.py, 行 409-413
def _get_tts_params_by_category(self, category: str, crisis: bool) -> dict:
    if crisis:
        return CATEGORY_TTS_PARAMS_MAP['crisis']   # 高危 → 最高优先级
    return CATEGORY_TTS_PARAMS_MAP.get(category, CATEGORY_TTS_PARAMS_MAP['neutral'])
```

**优先级**：
1. `crisis == True` → 强制使用 `crisis` 参数（最安抚的配置）
2. 否则 → 使用路由 LLM 判定的 `category` 对应的参数
3. 如果 category 不在映射表中 → 回退到 `neutral` 参数

### 3.5 参数流转路径

```
路由 LLM (DeepSeek)
   │
   ├─ 判定 category (depression/anxiety/anger/loneliness/grief/positive/neutral/crisis)
   └─ 判定 crisis (bool)
          │
          ▼
LLMService._get_tts_params_by_category(category, crisis)
          │
          ├─ crisis=True → CATEGORY_TTS_PARAMS_MAP['crisis']
          └─ crisis=False → CATEGORY_TTS_PARAMS_MAP[category]
          │
          ▼
SSE "done" 事件:
{
  "type": "done",
  "full_text": "...",
  "category": "depression",
  "strategy_id": "validate_feeling",
  "strategy_name": "共情与确认",
  "tts_params": { "speed": 0.82, "pitch": -2, "style": "gentle" }
}
          │
          ▼
前端 useLLM → 提取 tts_params → 传给 useTTS.speak(text, ttsParams, emotionLabel)
          │
          ▼
前端 useTTS → POST /tts/stream { text, speed, pitch, style, emotion_label }
          │
          ▼
后端 TTSService.synthesize_stream() → CosyVoice API → PCM 音频流
```

---

## 4. 前端 TTS 播放

### 4.1 useTTS Hook (`mobile/hooks/useTTS.ts`)

#### 4.1.1 数据结构

```typescript
interface QueueItem {
  text: string;           // 待合成文本
  ttsParams: TTSParams;   // { speed: number; pitch: number; style: string }
  emotionLabel: string;   // 用户情绪标签
}

interface TTSParams {
  speed: number;   // 语速 0.5~2.0
  pitch: number;   // 音调 -12~12
  style: string;   // 风格 gentle/calm/cheerful/neutral
}

interface UseTTSOptions {
  apiBase?: string;               // 后端地址，默认 http://localhost:8050
  onPlaybackDone?: () => void;    // 播放完成回调
}
```

#### 4.1.2 公开 API

| 方法 | 签名 | 说明 |
|------|------|------|
| `speak` | `(text: string, ttsParams?: TTSParams, emotionLabel?: string) => void` | 将文本加入播放队列 |
| `stop` | `() => void` | 清空队列，关闭 AudioContext |
| `isPlaying` | `boolean` (state) | 当前是否正在播放 |

#### 4.1.3 内部实现

##### playOnce() — 单次合成与播放

```typescript
async function playOnce(text: string, ttsParams: TTSParams, emotionLabel: string): Promise<void>
```

**流程**：

1. **发起 HTTP 请求**：
   ```typescript
   const response = await fetch(`${baseUrl}/tts/stream`, {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({
       text,
       speed: ttsParams.speed,
       pitch: ttsParams.pitch,
       style: ttsParams.style,
       emotion_label: emotionLabel,
     }),
   });
   ```

2. **流式读取 PCM 数据**：
   ```typescript
   const reader = response.body!.getReader();
   while (true) {
     const { done, value } = await reader.read();
     if (done) break;
     // value 是 Uint8Array（PCM 16bit LE）
   }
   ```

3. **PCM 格式转换**：
   ```typescript
   const int16 = new Int16Array(uint8.buffer);
   const float32 = new Float32Array(int16.length);
   for (let i = 0; i < int16.length; i++) {
     float32[i] = int16[i] / 32768.0;  // Int16 → Float32 [-1.0, 1.0]
   }
   ```

4. **创建 AudioBuffer 并调度播放**：
   ```typescript
   const audioBuffer = audioCtx.createBuffer(1, float32.length, 24000);
   audioBuffer.getChannelData(0).set(float32);
   const source = audioCtx.createBufferSource();
   source.buffer = audioBuffer;
   source.connect(audioCtx.destination);
   source.start(startTime);  // 精确时间调度，避免片段间间隙
   ```

##### processQueue() — 顺序播放队列

```typescript
async function processQueue(): Promise<void>
```

- 从 `playQueueRef` 逐个取出 `QueueItem`
- 依次调用 `playOnce()`，等待每个播放完成
- 队列为空后：设置 `isPlaying = false`，派发 `tts-end` 事件

##### AudioContext 管理

- **懒初始化**：`AudioContext` 在首次 `speak()` 调用时创建
- **采样率**：固定使用 24kHz（`new AudioContext({ sampleRate: 24000 })`）
- **销毁**：`stop()` 时关闭 AudioContext，释放音频资源

### 4.2 useLLM → useTTS 桥接 (`mobile/hooks/useLLM.ts`)

```typescript
interface UseLLMOptions {
  baseUrl?: string;
  onSentence?: (sentence: string, ttsParams: TTSParams, category: string) => void;
  onDelta?: (deltaText: string) => void;
  onDone?: (strategyName?: string) => void;
}
```

**关键桥接逻辑**（`fetchReply` 函数内）：

1. 解析 SSE 事件流
2. 遇到 `type: "done"` 时，提取 `data.tts_params`：
   ```typescript
   if (data.tts_params) {
     ttsParams = {
       speed: data.tts_params.speed ?? 1.0,
       pitch: data.tts_params.pitch ?? 0,
       style: data.tts_params.style ?? 'neutral',
     };
   }
   ```
3. SSE 流结束后，调用回调触发 TTS：
   ```typescript
   onSentence?.(fullText.trim(), ttsParams, category);
   ```

### 4.3 回音消除机制

TTS 播放期间必须静音麦克风输入，防止播放的语音被 ASR 重新拾取造成回声循环。

**机制**：使用全局 CustomEvent 进行跨组件通信

```
TTS 开始播放
    │
    ├─ useTTS.processQueue() 派发 window 'tts-start' 事件
    │       │
    │       ▼
    │   useASR 监听 'tts-start' → isTTSMutedRef.current = true
    │       │
    │       ▼
    │   ASR onaudioprocess 每帧检查 isTTSMutedRef → 跳过音频发送
    │
    ... TTS 播放中 ...
    │
TTS 播放结束
    │
    ├─ useTTS.processQueue() 派发 window 'tts-end' 事件
    │       │
    │       ▼
    │   useASR 监听 'tts-end' → 200ms 延迟后 → isTTSMutedRef.current = false
    │       │
    │       ▼
    │   ASR 恢复麦克风音频发送
```

**200ms 延迟的原因**：给房间混响（reverb）留出消散时间，避免 TTS 尾音被捕捉。

**前置条件**：Web Audio API 的 `getUserMedia` 也设置了 `echoCancellation: true` 作为硬件层面的第一道防线。

---

## 5. API 参考

### 5.1 POST `/tts/stream` — 流式语音合成

| 属性 | 值 |
|------|-----|
| **URL** | `POST http://localhost:8050/tts/stream` |
| **Content-Type** | `application/json` |
| **Response Content-Type** | `audio/pcm` |

**请求体** (JSON):

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `text` | string | ✅ | — | 待合成文本 |
| `speed` | float | ❌ | `1.0` | 语速倍率，范围 0.5~2.0 |
| `pitch` | int | ❌ | `0` | 音调偏移（半音），范围 -12~12 |
| `style` | string | ❌ | `"neutral"` | 情感风格: `gentle`, `calm`, `cheerful`, `neutral` |
| `emotion_label` | string | ❌ | `"neutral"` | 用户情绪标签（用于音色选择） |

**响应头**:

| 头名称 | 值 | 说明 |
|--------|-----|------|
| `Content-Type` | `audio/pcm` | 原始 PCM 音频 |
| `X-Sample-Rate` | `24000` | 采样率 24kHz |
| `X-Channels` | `1` | 单声道 |
| `X-Bit-Depth` | `16` | 16 位深度 |
| `Cache-Control` | `no-cache` | 禁止缓存 |
| `X-Accel-Buffering` | `no` | 禁用 Nginx 缓冲 |

**请求示例**:

```json
{
  "text": "我理解你的感受。这不是你的错。",
  "speed": 0.82,
  "pitch": -2,
  "style": "gentle",
  "emotion_label": "sad"
}
```

**错误响应**:
- `400 Bad Request` — `text` 为空或参数超出范围
- `503 Service Unavailable` — TTS 服务未初始化

### 5.2 GET `/tts/status` — 状态查询

| 属性 | 值 |
|------|-----|
| **URL** | `GET http://localhost:8050/tts/status` |

**响应示例**:

```json
{
  "is_playing": true
}
```

若服务未初始化：
```json
{
  "is_playing": false,
  "status": "not_initialized"
}
```

### 5.3 SSE `done` 事件 — TTS 参数字段

当 LLM 流式回复完成时，SSE `/llm/stream` 会发送一个 `done` 事件，其中包含 `tts_params` 字段：

```json
{
  "type": "done",
  "full_text": "我理解你现在的感受...",
  "crisis": false,
  "category": "depression",
  "strategy_id": "validate_feeling",
  "strategy_name": "共情与确认",
  "tts_params": {
    "speed": 0.82,
    "pitch": -2,
    "style": "gentle"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `tts_params.speed` | float | 语速倍率 0.5~2.0 |
| `tts_params.pitch` | int | 音调偏移 -12~12 |
| `tts_params.style` | string | 风格标签: gentle/calm/cheerful/neutral |

---

## 6. 附录

### 6.1 配置常量速查表

| 常量 | 值 | 位置 | 说明 |
|------|-----|------|------|
| `TTS_MODEL` | `cosyvoice-v1` | `config.py:174` | CosyVoice 模型名 |
| `TTS_DEFAULT_VOICE` | `longxiaochun` | `config.py:175` | 默认音色（环境变量可覆盖） |
| `TTS_SAMPLE_RATE` | `24000` | `config.py:178` | 输出采样率 |
| `TTS_CHANNELS` | `1` | `config.py:179` | 输出声道数 |
| `TTS_BIT_DEPTH` | `16` | `config.py:180` | 输出位深度 |
| `TTS_SPEED_MIN` | `0.5` | `config.py:183` | 最小语速 |
| `TTS_SPEED_MAX` | `2.0` | `config.py:184` | 最大语速 |
| `TTS_PITCH_MIN` | `-12` | `config.py:185` | 最低音调（半音） |
| `TTS_PITCH_MAX` | `12` | `config.py:186` | 最高音调（半音） |
| `TTS_MAX_SENTENCE_LEN` | `50` | `config.py:209` | 分句参考阈值 |
| `DASHSCOPE_API_KEY` | 环境变量 | `config.py:99` | DashScope API 密钥 |

### 6.2 参数映射速查表

#### CATEGORY_TTS_PARAMS_MAP（当前主路径）

| 心理类别 | speed | pitch | style | 中文 |
|----------|-------|-------|-------|------|
| depression | 0.82 | -2 | gentle | 抑郁 |
| anxiety | 0.88 | -1 | calm | 焦虑 |
| anger | 0.88 | -1 | gentle | 愤怒 |
| loneliness | 0.85 | -2 | gentle | 孤独 |
| grief | 0.82 | -2 | gentle | 哀伤 |
| positive | 1.05 | +1 | cheerful | 积极 |
| neutral | 1.00 | 0 | neutral | 中性 |
| crisis | 0.82 | -2 | gentle | 高危 |

#### TTS_STYLE_MAP

| 系统风格 | CosyVoice 标签 |
|----------|----------------|
| gentle | gentle |
| calm | calm |
| cheerful | cheerful |
| neutral | neutral |

#### TTS_EMOTION_VOICE_MAP

| 情绪 | 音色 |
|------|------|
| happy / neutral / sad / fearful / angry / disgusted / surprised / _crisis | longxiaochun |

### 6.3 关键文件索引

| 文件 | 说明 |
|------|------|
| `backend/config.py` (行 147-209) | 全部 TTS 配置 |
| `backend/services/tts_service.py` | TTS 核心服务实现 |
| `backend/routers/stream_tts.py` | TTS HTTP 路由 |
| `backend/services/llm_service.py` (行 402-413) | TTS 参数决策逻辑 |
| `backend/routers/sse_llm.py` (行 27-56) | SSE 流式 LLM 端点 |
| `backend/main.py` (行 32, 39) | 服务初始化与注入 |
| `mobile/hooks/useTTS.ts` | 前端 TTS 播放 Hook |
| `mobile/hooks/useLLM.ts` | 前端 SSE 接收与 TTS 桥接 |
| `mobile/hooks/useASR.ts` (行 144-171) | 回音消除（TTS 静音联动） |
| `mobile/constants/TTS.ts` | 前端 TTS 常量 |

### 6.4 依赖项

| 依赖 | 版本/说明 | 用途 |
|------|-----------|------|
| `dashscope` | 阿里云 DashScope Python SDK | TTS API 调用 |
| `SpeechSynthesizer` | `dashscope.audio.tts_v2` | CosyVoice 合成器 |
| `AudioFormat` | `dashscope.audio.tts_v2` | 音频格式枚举 |
| `ResultCallback` | `dashscope.audio.tts_v2` | 异步回调基类 |
